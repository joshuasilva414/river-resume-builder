# River v1.2 hosted acceptance

In progress on 2026-09-08 UTC. All newly entered content in this acceptance run is fictional and labeled as test data. Existing account content is preserved. This record complements [implementation status](v1.2.md) and [deployment/recovery evidence](v1.2-staging-recovery.md).

## Environment and execution

The existing Chrome session opens authenticated staging. The separate in-app browser required sign-in; it was not used to infer hosted acceptance. The user restored the saved OpenAI connection and selected `gpt-5.6-luna` as its default through River's Settings. No credential was read or copied by the agent.

PR #16 passed application and document jobs on `62c9691855c2c4c9d06faca3d30a50b8d9d657fe` and merged into dev at `f6f962467c2bb4395d736f3b26b4be42d0a9d44b`. PR #17 passed both promotion jobs and merged into staging at `3fdd03802aaaaa56064723fc6187154041f30c32`. Cloudflare build `365a1e28-e114-43f1-889b-af96a2385905` completed successfully at `2026-09-08T10:03:55.997Z`; its versions and independent checks are in the recovery record. Logs are retained under `test-results/v1.2-acceptance/pr16-ci.log` and `pr17-ci.log`.

The subsequent corrections passed application and document CI on `bcc9ed3615deb0ef1a25ee7a816d25b2f897cbb1` and merged through PR #18 into dev as `bae914e512b9318deefca6c5aeb917820d5b5ce2`. Staging promotion PR #19 passed both PR and dev-push checks but remains open. These fixes are not yet part of the deployed version above. Retained CI logs: `test-results/v1.2-acceptance/pr18-ci.log` and `pr19-ci.log`.

## Observed hosted results

- The initial fictional pasted-job request failed provider authorization. Its complete posting text remained available for editing and manual review.
- After the user's connection update, Analyze again succeeded with `gpt-5.6-luna`. The generated review contained three Qualifications and two Eligibility items. One qualification was edited, one eligibility item deselected, and Save created the job and four requirements together. No false stale warning appeared. Eligibility is informational and résumé creation remains available.
- Saved fictional job: `01a0806f-2535-7696-9ddd-3e8178f056ab`. New fictional résumé: `01a08074-0d7f-706a-9706-b8ab73e147f8`.
- The repository's fictional `representative.docx` uploaded and reached Ready. Extracted text retained accents, Chinese text, repeated passages, and the TypeScript/SQL accomplishment. Its duplicate-original notice was accurate and preserved both originals.
- New fictional DOCX source: `01a08071-8ae4-71fd-832a-ed82f20fd33c`. Automatic evidence extraction with `gpt-5.6-luna` and an explicit new request with `gpt-5.4-mini` both returned HTTP 400 provider request rejection. The provider schema incorrectly included optional `material.sourceIds`. The local correction omits that redundant model-generated field; validated citations still determine source associations. All 19 source tests passed, including the actual SDK request shape with zero or selected contexts. Hosted retesting remains pending.
- A new template's real two-page sample PDF rendered. A live chat instruction to add an optional Recognition field failed before compilation while retaining the working copy and previous PDF. The provider schema incorrectly made `composition` optional. The local correction makes it required and nullable only in provider requests, preserving older persisted readers. The error message now describes task failure without claiming the compiler ran. The focused template/schema-transition Workers batch passed 15 tests, including recursive strict-schema checks and omitted/null historical output. Hosted retesting remains pending.
- A direct fictional Summary saved in one operation. A generated Contact form saved fictional name/email without separate content/block creation.
- Switching a new generic section from Summary fields to Contact fields revealed that its saved type stayed Summary, so résumé preview could not recognize the name. The local fix exposes Content type for new items and derives it when selecting a built-in schema. Existing items retain immutable types. A rendering test proves the switched Contact becomes the résumé header while retaining prior fields. A separate Contact starter was used to continue independent export acceptance; the faulty fixture is not treated as a passing preview.
- Both public Greenhouse and Ashby URL imports failed during DNS validation before fetching the posting. Cloudflare Workers rejects `redirect: "error"`. The correction uses `manual`; non-success DNS responses still fail closed. The regression reproduced the exact Workers exception before correction. All 11 job-import tests, TypeScript, and Biome passed, including rejection of a DNS resolver redirect. Hosted URL retries await deployment.
- The correct Contact starter compiled a valid one-page PDF with the fictional name/email. Its protected PDF opened in Chrome's native viewer, but the existing résumé tab failed during embedded PDF loading. A full page reload restored the embedded preview, showing Current and enabling Add. The tab predated a deployment, so a stale lazy asset is a possible cause; no browser HTTP failure was captured. No speculative viewer change was made.

## Saved version, wording, and scoring

The Contact and Summary sections were each previewed in the actual résumé before insertion. Editing Summary retained the previous PDF and disabled Add until the new PDF displayed. The inserted text persisted without changing the saved library Summary.

Live wording generation succeeded with `gpt-5.6-luna`. Its suggestion was edited in the complete-result queue and applied through Select all / Apply selected. The record became Accepted, the draft retained the edited text, and no false stale warning appeared. This is a one-choice browser batch; cross-page/mixed-target atomicity is established separately by Workers tests.

Save & score captured checkpoint `01a0808b-842a-7cda-807e-3041f030bba8`. PDF operation `01a0808b-842a-7df6-bd30-4ba242166f10` passed preparation, template, and wording checks. The one-page rendered PDF was visually inspected: fictional name/email and the exact edited Summary were present without overlap. Export completed at `2026-09-08T10:24:10.782Z`. The Download PDF link's browser download action completed successfully. Its retained artifact manifest is included in the recovery record; local byte rehash is reserved for the final drill.

Two explicit scoring requests each completed on their first attempt and saved all six provider simulations. Read-only D1 checks found two Consumed reservations, both marked administrator-exempt, and a daily successful-result count of two. This verifies successful result accounting and administrator cap exemption. These are distinct results, not a reuse test. Scores describe provider simulations, not real vendor screening behavior.

The fictional reusable Summary was moved to Trash after export. Reloading the saved version retained Passed checks, the same export operation/link, and the exact edited Summary in extracted text. The section was then restored from the searchable Trash screen. No private or pre-existing item was changed. A separate fictional Experience section also saved its nested entry and accomplishment together, preserving a year-only start date, Present end date, and a switch from Classic to Compact without content loss.

## Import cancellation and recovery

After the earlier failed URL request, Paste description accepted fictional replacement text and live analysis reached an editable review. A subsequent Analyze again request was cancelled immediately after submission. The interface reported cancellation, retained the text, and stayed cancelled after the delayed submission returned. Enter details manually then showed no generated requirements. A manually entered qualification and edited fictional role/source URL saved as job `01a08095-be9e-772a-bedc-d45cb0bd4d18`. No cancelled result was applied. This demonstrates retrieval-failure paste recovery and submission cancellation; the original URL failure was River's DNS bug, not a verified blocked publisher response.

Refreshing that job with pasted text produced a live analyzed review. Editing its description to add a preferred qualification displayed the relevant-input warning and disabled Save. After adding the qualification and explicitly reviewing the requirements, Save enabled and atomically retained the refreshed posting and five requirements. Changing only its role label did not trigger the warning. The fictional reference URL was cleared using ordinary keyboard input before analysis.

Further source, template, URL retrieval, and recovery results will be recorded after their actual execution. Automated and local browser evidence remains separate from these hosted results.
