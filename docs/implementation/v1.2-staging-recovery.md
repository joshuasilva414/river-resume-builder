# River v1.2 staging deployment and recovery

Status: the initial staging deployment/recovery and authorized field-picker follow-up deployment passed. Signed-in browser and live-provider acceptance are in progress. A further bug-fix promotion is pending after issues found during hosted acceptance. A new populated-record recovery drill is held until the release owner signals that hosted fixtures are ready. Production is outside this work.

The initial feature commit is `f1fd5338f7aa9062fcc1b51ce4b204b3c2b683e4`. PR #14 merged it into dev at `cd1e9a8754fdfc9d8c3d03c0dc967d99098cd1ff`. PR #15 promoted dev to staging as `9815b0b44ae3b4973cdbdbda632185abeda465ab`; the Cloudflare build API confirms that exact staging merge commit.

The initial deployment and recovery below remain historical evidence for the first v1.2 build. Signed-in staging and live-provider acceptance are separate from deployment and recovery checks.

## Authorized follow-up deployment

PR #16 merged the field-picker follow-up into dev at `f6f962467c2bb4395d736f3b26b4be42d0a9d44b`. PR #17 promoted dev to staging as `3fdd03802aaaaa56064723fc6187154041f30c32` at `2026-09-08T09:50:58Z`.

Cloudflare build `365a1e28-e114-43f1-889b-af96a2385905`, created at `2026-09-08T09:51:00.638Z`, succeeded at `2026-09-08T10:03:55.997Z` for that exact staging commit. Its logs confirm type checking, lint, 261 Workers tests, 43 template tests, document compiler fixtures, and successful automatic deployment. No manual deployment was run.

Independent API reads confirmed these active deployments at 100 percent:

| Worker | Deployment | Version | Created UTC |
| --- | --- | --- | --- |
| `river-documents-staging` | `137c44bc-3833-4d23-9537-5cb29d6b9c80` | `6c4f0b46-0fb5-4fb8-9a27-baf104a13d26` | `2026-09-08T10:03:26.519418Z` |
| `river-staging` | `ae2eed70-a9b2-450d-99a9-22ba0496f87c` | `2c3c2320-a685-49cb-8e50-be5355e4fc03` | `2026-09-08T10:03:51.747551Z` |

Follow-up settings match the staging D1/R2, browser, document service, eleven workflows, backup tuple, telemetry configuration, and document-container namespace listed below. The encrypted-credential secret binding is present; no secret value was inspected or changed by this observation. At `2026-09-08T10:05:11Z`, `/sign-in` and `/docs` returned 200, anonymous `/api/v1/me` returned 401, and anonymous `/admin` and `/trash` redirected to `/sign-in`.

Safe read-only receipts are `test-results/v1.2-acceptance/recovery/follow-up-deployments.json`, `follow-up-bindings-check.json`, and `follow-up-routes.json`. The release owner reports a restored personal connection and successful live job import. Subsequent hosted checks found source/template provider-schema and section-switching issues, with fixes and another promotion pending. This deployment receipt does not mark those acceptance paths complete.

A fresh populated-data backup and isolated recovery will follow only after the hosted fictional acceptance records are ready. No manual backup or restore was started for this follow-up; the initial snapshot below does not contain those new records.

### Populated-record readiness snapshot

Read-only D1 inspection on 2026-09-08T10:23:35.774Z compared current counts with the `09:27:20.470 UTC` backup snapshot. PR #18's final fixes remain in CI and are not covered by a later deployment receipt here. All successful inspection statements wrote zero rows; no source, résumé, provider payload, or credential value was returned.

| New coverage since the initial backup | Observed state |
| --- | --- |
| `job_imports` | Four records: one Succeeded and saved with retained text/analysis; three Failed, including one with retained text. One new job and posting snapshot are retained. |
| Source and processing history | One new Ready source and processing result. Its selected AI connection/model reference is saved. Two extraction attempts are Failed with no candidates or accepted evidence. |
| Structured reusable content | Three new `library_items` and three `library_revisions`, all structured sections. |
| Structured résumé and preview | One new résumé, revision 2, containing two structured sections. Its preview succeeded with document artifacts. No saved version has been created for it. |
| Template history | One new conversation and one Failed template AI task, with no retained result. No new template revision, composable graph, validation, or automatic-approval request. |
| Scoring and administration | No new scoring or template-scoring runs. Overrides, reservations, reset receipts, and reset audit entries remain empty. Default policy remains 25 at revision 0. |

Fixture references: source `01a08071-8ae4-71fd-832a-ed82f20fd33c`; saved job `01a0806f-2535-7696-9ddd-3e8178f056ab` with three Qualifications and one Eligibility; résumé `01a08074-0d7f-706a-9706-b8ab73e147f8`; successful preview `01a08089-722a-77e9-a797-c0b2eb5580d6`. The saved import is `01a0806e-80a7-7c77-89b4-2ab21ced437f`.

This is a readiness observation, not recovery proof. The final drill can add live coverage for imports, selected source AI, structured library/résumé data, and the preview, but successful extraction, custom-template approval, saved-version/export, and quota-control records still need acceptance work before they can contribute populated recovery coverage. Backup and restore remain held for the release owner's final signal. Safe counts and statuses are retained in `test-results/v1.2-acceptance/recovery/follow-up-readiness.json`.

### Saved export and two successful scoring results

A later read-only inspection at 2026-09-08T10:28:34.976Z adds populated saved-version/export and scoring evidence to the earlier readiness snapshot. Checkpoint `01a0808b-842a-7cda-807e-3041f030bba8` captured résumé revision 3 with two structured sections at `2026-09-08T10:23:42.122Z`. Its document operation `01a0808b-842a-7df6-bd30-4ba242166f10` is Succeeded, stage PDF ready, with `validationPassed=true` and retained PDF/LaTeX/text/report keys.

The export was retained at `2026-09-08T10:24:10.782Z` under `river-evidence-export-v2`, with zero review issues. Export review digest: `65583a672298652fd696db753e8aafebae461928b360683675f95af66a0b6de9`. The release owner separately observed the rendered PDF and completed browser export; this read-only pass inspected metadata.

| Scoring run | Completed UTC | Simulations | Reservation | Administrator exempt |
| --- | --- | --- | --- | --- |
| `01a0808b-8473-7af3-bdff-d71c636a94e8` | `2026-09-08T10:24:05.856Z` | 6 | Consumed | Yes |
| `01a0808d-71d8-77e7-8679-6a0c2f3e8867` | `2026-09-08T10:26:03.481Z` | 6 | Consumed | Yes |

These are two distinct successful provider results, each completed on its first attempt with a distinct result digest and operation. The second explicit Score action created a new run; it is not evidence of retained-result reuse. The account's `2026-09-08` usage row is exactly 2, and its reservation ledger contains exactly two Consumed rows, both `exempt=1`. Six platform simulations count as one checkpoint scoring result. Administrator activity remains measurable while bypassing the daily cap; the default ordinary-account policy remains 25. This does not exercise ordinary-account exhaustion or a live allowance reset.

The retained artifact manifest records these SHA-256 values:

| Artifact | SHA-256 |
| --- | --- |
| PDF | `88310f3cec4030d39bcf6f905ee186e6db1f3ce3dc3ac9d6d8ca4bec1b6c580c` |
| LaTeX | `ba1200b0a529c457b742f37b1bdd1da98d702d9309056b6da6cd2ea32fa123da` |
| Plain text | `8a527da298af8097b28d239cfa210b1d43bb765198f40bc46d75b9b596924cf2` |
| Validation report | `57f9cfd97b006a23d0f53d900807b19e9a7e717b4510c8464f9769e44c092e44` |

These hashes were captured from the retained D1 manifest. The objects were not downloaded or rehashed by this metadata pass; that verification remains part of the final recovery drill. Safe IDs, statuses, times, counts, and digests are retained in `test-results/v1.2-acceptance/recovery/follow-up-checkpoint-scoring.json`. Backup and restore remain on hold.

## Initial deployment

Cloudflare build `b810696e-618a-4638-9c21-8e1b26afc39d` was created at `2026-09-08T09:13:06.283Z` for that exact staging commit and completed successfully at `2026-09-08T09:23:29.787Z`. Its commands were `pnpm ci:build staging` and `pnpm ci:deploy staging`. The build log confirmed 261 passing Workers tests, 43 passing template tests, and ten successful test tasks. The final deploy log identifies the current web version and confirms its sign-in and anonymous API checks. No manual deployment or retry was started during this observation.

### Initial deployment resources

Independent Cloudflare API reads confirmed both active deployments at 100 percent:

| Worker | Deployment | Version | Created UTC |
| --- | --- | --- | --- |
| `river-documents-staging` | `b2ff81d2-6361-4835-ae49-ef3106e23298` | `20bece6c-0688-48e4-85c4-b2db7e184395` | `2026-09-08T09:23:02.941685Z` |
| `river-staging` | `d0d29e01-b3b8-4f3b-ae41-7f643dee0058` | `1a95dbba-6d5a-4d16-8a00-eed11f8ab13b` | `2026-09-08T09:23:25.865862Z` |

Live settings match the staging source configuration: D1 `1b837147-de99-4aaa-a4ad-386b826412b0`, R2 `river-staging-artifacts`, `DOCUMENTS` service `river-documents-staging` with `DocumentService`, `BROWSER`, and all eleven workflow names/classes. The backup account/database/bucket tuple also matches. Document Worker `CONTAINER` remains bound to `DocumentContainer`, namespace `884d8dd4da43419286b646a4dc1c253b`. Both Workers use compatibility date `2026-08-22` and `nodejs_compat`.

Query-string redaction is enabled; persistent/invocation logs and traces are disabled on the web Worker, with `river-telemetry-staging` as its tail consumer. The `AI_CREDENTIAL_ENCRYPTION_KEY` secret binding is present. Its value was not fetched, printed, or changed. Preserving that matching key remains required to use restored encrypted connections; this drill does not decrypt or reconnect them.

D1 reports 39 applied migrations. All five v1.2 migrations (`0034` through `0038`) applied at `2026-09-08 09:22:56 UTC`. The independent migration reads wrote zero rows.

At `2026-09-08T09:25:09Z`, public route checks returned `/sign-in` 200, `/docs` 200, and anonymous `/api/v1/me` 401. Anonymous `/admin` and `/trash` redirected to `/sign-in`, which returned 200. These checks do not establish signed-in administration or browser/provider acceptance.

## Initial post-migration backup and restore

The first manual backup attempt failed at its initial Wrangler D1 read, before export. Its output was withheld by the existing recovery wrapper. A read-only `SELECT 1` probe succeeded, and one fresh backup retry succeeded. No additional cause was recovered; the initial failure must not be characterized as an application data error.

- Backup ID: `2026-09-08T09-27-14.507Z-fd60e257-59e7-42a3-b3e5-d51c6ee4660e`.
- Private R2 key: `backups/database/staging/2026-09-08T09-27-14.507Z-fd60e257-59e7-42a3-b3e5-d51c6ee4660e/snapshot.json.gz`.
- Snapshot SHA-256: `66f3bedff5093c7e4a86bba4bb0fbac19692ffaed76963ec4adc79a1dd551308`.
- Captured base tables: 82, including all v1.2 tables and migrations through `0038`.
- Receipt: `test-results/recovery/2026-09-08T09-27-14.507Z-fd60e257-59e7-42a3-b3e5-d51c6ee4660e/receipt.json`.
- Isolated restore directory: `test-results/recovery/drill-d4db41cc-ebc8-48b0-9abc-540a9765befc/`.

`pnpm restore:drill <absolute-receipt-path> --staging` completed at `2026-09-08T09:30:59.609Z` with exit 0 and `status: passed`. It restored all 82 tables and verified all 170 retained objects, 27 exact citations, five exported saved versions, and eight retained template validation fixtures. Its final report is `test-results/recovery/drill-d4db41cc-ebc8-48b0-9abc-540a9765befc/report.json`, SHA-256 `20a69e688f77dc320d295c24001bea4195fa62883f3e2cdbc68a00aa7bf7e7f6`. Object reads retained the bounded three-attempt recovery policy. The restored SQLite file remains local and unpublished, with no restored work dispatched.

An independent comparison loaded the raw export into an in-memory database without import-time triggers and compared sorted row hashes against the read-only restored database. All 82 base tables matched exactly. Integrity and foreign-key checks passed. Template/library/résumé and job-import JSON columns remained valid. This verifies stored JSON preservation without printing private values.

The restored quota policy retains the default limit 25 at revision 0. Two usage-day rows contain six historically seeded results. Account overrides, reservations, reset receipts, and reset audit entries are empty and remain empty. Admission, settlement, and release triggers are present, their hashes are retained, ranges are valid, and no Reserved entry points to a terminal operation.

This staging snapshot has no v1.2 composable template graph, URL/paste job-import record, automatic-approval request, or saved source model selection. It therefore establishes additive migration, historical preservation, and empty-state recovery for those new features. Populated v1.2 schema/override/reset/reservation behavior is covered separately by the focused synthetic recovery/scoring tests; it is not claimed as live use in this snapshot.

Safe acceptance evidence is retained under ignored `test-results/v1.2-acceptance/recovery/`. Private SQL and source/artifact bytes remain under the existing restricted recovery directories. Signed-in browser/provider acceptance is recorded separately by the release owner.

Evidence files: `deployments-bindings.json`, `migrations.json`, `routes.json`, `post-backup.log`, `d1-probe.log`, `post-backup-retry.log`, `post-restore.log`, `restored-invariants.json`, and `summary.json`. The summary retains receipt/report/invariant-file hashes. The ignored `verify-restored.mjs` records the exact read-only comparison and invariant queries used for this drill. No tracked application or recovery code changed in this verification task.
