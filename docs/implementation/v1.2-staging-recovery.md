# River v1.2 staging deployment and recovery

Status: staging deployment, hosted template and JavaScript-rendered job acceptance, final populated backup, complete isolated recovery, and independent invariants passed. Staging runs PR #23's commit `c27435724809bea10920d389d83f0582ba063084`; both Worker versions, bindings, and anonymous routes are verified. The final drill restored all 82 base tables and checked 242 retained objects, 58 exact citations, six exported saved versions, and 12 retained template fixtures. Production is outside this work.

The initial feature commit is `f1fd5338f7aa9062fcc1b51ce4b204b3c2b683e4`. PR #14 merged it into dev at `cd1e9a8754fdfc9d8c3d03c0dc967d99098cd1ff`. PR #15 promoted dev to staging as `9815b0b44ae3b4973cdbdbda632185abeda465ab`; the Cloudflare build API confirms that exact staging merge commit.

The initial deployment and recovery below remain historical evidence for the first v1.2 build. Signed-in staging and live-provider acceptance are separate from deployment and recovery checks.

## JavaScript retrieval and recovery-integrity deployment

PR #22 merged feature `53b46ea` into dev at `cd6bdd68a1d7ddfad4cb5fbe5e56e24e825dece7`. PR #23 promoted it to staging commit `c27435724809bea10920d389d83f0582ba063084` at `2026-09-08T12:09:04Z`. Cloudflare build `371add39-c54f-4abe-baa8-15ad217c0aa9`, created at `2026-09-08T12:09:06.662Z`, succeeded at `2026-09-08T12:22:59.211Z` for that exact staging commit. Its logs confirm type checking, lint, 274 Workers tests across 39 files, 43 template tests, all ten test tasks, and document compiler/custom-graph fixture checks. Automatic deployment reported no migrations to apply.

Independent Cloudflare API reads at `2026-09-08T12:24:10.543Z` confirmed both current deployments at 100 percent:

| Worker | Deployment | Version | Created UTC |
| --- | --- | --- | --- |
| `river-documents-staging` | `27ae5093-d12d-4fe1-9f4b-3a4cf9174802` | `fc0bdefc-30df-46dd-97c6-d67a8e862e36` | `2026-09-08T12:22:29.718986Z` |
| `river-staging` | `79868a03-9809-463d-acd4-d1f873e20f4d` | `656455d2-0458-41f1-a6be-fe1be838ff75` | `2026-09-08T12:22:53.093659Z` |

The live binding check passed for pinned staging D1/R2, document service, browser, eleven workflows, safe environment/backup tuple, telemetry, document-container namespace, compatibility date, and `nodejs_compat`. The encrypted-credential secret binding is present; its value was not fetched or changed. The release owner's unrestricted `curl -L` probes passed: `/sign-in` and `/docs` returned 200; `/api/v1/me` returned 401; `/admin` and `/trash` redirected to `/sign-in`, returning 200. These route results were recorded at `2026-09-08T12:25:05.132Z` and are explicitly attributed to the release owner.

Safe receipts are `test-results/v1.2-acceptance/recovery/follow-up-js-recovery-deployments.json`, `follow-up-js-recovery-bindings-check.json`, and `follow-up-js-recovery-routes.json`. No manual deployment, backup, or restore was started. Hosted JavaScript-rendered job import is being retried on this version; final populated backup remains held until that acceptance succeeds.

## Template/public-URL correction deployment

PR #20 merged feature `e7ae112` into dev at `cd202dad2de4c809bb0d37686b83b46d794efe15`. PR #21 promoted it to staging commit `e9c37b4d4dbad160dcf8b4f0f597762d8190cbb3` at `2026-09-08T11:32:38Z`. Cloudflare build `b5609b4d-6968-4f5c-9933-2bbcd99512c7`, created at `2026-09-08T11:32:41.062Z`, succeeded at `2026-09-08T11:44:10.437Z` for that exact staging commit. Its logs confirm type checking, lint, 272 Workers tests across 39 files, 43 template tests, all ten test tasks, and the document compiler/custom-graph fixture checks. Automatic deployment reported no migrations to apply.

Independent Cloudflare API reads at `2026-09-08T11:45:38.220Z` confirmed both current deployments at 100 percent:

| Worker | Deployment | Version | Created UTC |
| --- | --- | --- | --- |
| `river-documents-staging` | `169046fd-1622-47c7-b207-558a9184858f` | `187f5565-c798-470d-b2f9-51c5247620de` | `2026-09-08T11:43:38.160899Z` |
| `river-staging` | `138bc7af-05b1-43da-8e57-fff08d4205be` | `dc195125-85be-4c5f-aafb-55d4de681f22` | `2026-09-08T11:44:04.47752Z` |

Live settings match the pinned staging D1/R2, document service, browser, all eleven workflows, safe environment/backup tuple, telemetry policy, document-container namespace, compatibility date, and `nodejs_compat`. The encrypted-credential secret binding is present; its value was not inspected or changed. Safe receipts are `test-results/v1.2-acceptance/recovery/follow-up-template-url-deployments.json`, `follow-up-template-url-bindings-check.json`, and `follow-up-template-url-routes.json`.

The release owner's unrestricted `curl -L` checks passed for this new version: `/sign-in` and `/docs` returned 200; `/api/v1/me` returned 401; `/admin` and `/trash` redirected to `/sign-in`, returning 200. The route receipt records the report time as `2026-09-08T11:47:58.795Z` and explicitly attributes these checks to the release owner. No signed-in or provider acceptance is implied by these anonymous checks.

No manual deployment, backup, or restore was started. The release owner was notified when the new web version became active and is retrying hosted template/public-URL acceptance. Final backup remains held until the fixtures are ready.

## Hosted-acceptance correction deployment

PR #19 promoted the approved corrections to staging commit `581c7a4a4651c1a37b5785042c9a25b31913663e` at `2026-09-08T10:47:19Z`. Automatic Cloudflare build `05274891-8fd7-4279-9227-1d423c4bf3eb` was created at `2026-09-08T10:47:21.782Z` for that exact commit and succeeded at `2026-09-08T10:59:44.386Z`. Its logs confirm type checking, lint, 268 Workers tests across 39 files, 43 template tests, all ten test tasks, and document compiler checks. Compiler coverage includes the custom graph's four canonical fixtures, inherited styles, cross-pack bindings, repeat rendering, and structured/columns fixtures. Deployment reported no migrations to apply.

Independent Cloudflare API reads at `2026-09-08T11:00:20.379Z` confirmed both current deployments at 100 percent:

| Worker | Deployment | Version | Created UTC |
| --- | --- | --- | --- |
| `river-documents-staging` | `6a9bd3c3-fb72-4075-8ade-4df4b68084ab` | `d163cb1b-9006-4520-b3e7-60b80146b509` | `2026-09-08T10:59:11.296687Z` |
| `river-staging` | `de14a36b-11c8-4ad9-aadc-0dd4630772c8` | `aeb74b5c-06ed-4227-a369-6b1da9f489a5` | `2026-09-08T10:59:39.812826Z` |

Live settings match the staging D1/R2, document service, browser, all eleven workflow names/classes, safe environment and backup tuple, telemetry policy, document-container namespace, compatibility date, and `nodejs_compat` configuration. The encrypted-credential secret binding remains present; its value was not fetched or changed.

The release owner independently checked all five anonymous routes with unrestricted `curl -L`: `/sign-in` and `/docs` returned 200; `/api/v1/me` returned 401; `/admin` and `/trash` redirected to `/sign-in`, returning 200. These results were recorded at `2026-09-08T11:02:02.895795Z`. The observer's local curl probe failed DNS resolution before receiving any HTTP response, so route evidence is explicitly attributed to the release owner's independent check.

Safe receipts are `test-results/v1.2-acceptance/recovery/follow-up-final-fixes-deployments.json`, `follow-up-final-fixes-bindings-check.json`, and `follow-up-final-fixes-routes.json`. No manual deployment, backup, or restore was run for this correction. Corrected source/template/public-URL browser acceptance remains separate and in progress; the final populated-record backup and isolated recovery remain on hold for the release owner's signal.

## Authorized follow-up deployment

PR #16 merged the field-picker follow-up into dev at `f6f962467c2bb4395d736f3b26b4be42d0a9d44b`. PR #17 promoted dev to staging as `3fdd03802aaaaa56064723fc6187154041f30c32` at `2026-09-08T09:50:58Z`.

Cloudflare build `365a1e28-e114-43f1-889b-af96a2385905`, created at `2026-09-08T09:51:00.638Z`, succeeded at `2026-09-08T10:03:55.997Z` for that exact staging commit. Its logs confirm type checking, lint, 261 Workers tests, 43 template tests, document compiler fixtures, and successful automatic deployment. No manual deployment was run.

Independent API reads confirmed these active deployments at 100 percent:

| Worker | Deployment | Version | Created UTC |
| --- | --- | --- | --- |
| `river-documents-staging` | `137c44bc-3833-4d23-9537-5cb29d6b9c80` | `6c4f0b46-0fb5-4fb8-9a27-baf104a13d26` | `2026-09-08T10:03:26.519418Z` |
| `river-staging` | `ae2eed70-a9b2-450d-99a9-22ba0496f87c` | `2c3c2320-a685-49cb-8e50-be5355e4fc03` | `2026-09-08T10:03:51.747551Z` |

Follow-up settings match the staging D1/R2, browser, document service, eleven workflows, backup tuple, telemetry configuration, and document-container namespace listed below. The encrypted-credential secret binding is present; no secret value was inspected or changed by this observation. At `2026-09-08T10:05:11Z`, `/sign-in` and `/docs` returned 200, anonymous `/api/v1/me` returned 401, and anonymous `/admin` and `/trash` redirected to `/sign-in`.

Safe read-only receipts are `test-results/v1.2-acceptance/recovery/follow-up-deployments.json`, `follow-up-bindings-check.json`, and `follow-up-routes.json`. The release owner reports a restored personal connection and successful live job import. Subsequent hosted checks found source/template provider-schema and section-switching issues, with fixes and another promotion pending. This deployment receipt does not mark those acceptance paths complete.

A fresh populated-data backup and isolated recovery will follow only after the hosted fictional acceptance records are ready. No manual backup or restore was started for this follow-up; the initial snapshot below does not contain those new records.

### Populated-record readiness snapshot

Read-only D1 inspection on 2026-09-08T10:23:35.774Z compared current counts with the `09:27:20.470 UTC` backup snapshot. PR #18's final fixes remain in CI and are not covered by a later deployment receipt here. All successful inspection statements wrote zero rows; no source, résumé, provider payload, or credential value was returned.

| New coverage since the initial backup | Observed state |
| --- | --- |
| `job_imports` | Four records: one Succeeded and saved with retained text/analysis; three Failed, including one with retained text. One new job and posting snapshot are retained. |
| Source and processing history | One new Ready source and processing result. Its selected AI connection/model reference is saved. Two extraction attempts are Failed with no candidates or accepted evidence. |
| Structured reusable content | Three new `library_items` and three `library_revisions`, all structured sections. |
| Structured résumé and preview | One new résumé, revision 2, containing two structured sections. Its preview succeeded with document artifacts. No saved version has been created for it. |
| Template history | One new conversation and one Failed template AI task, with no retained result. No new template revision, composable graph, validation, or automatic-approval request. |
| Scoring and administration | No new scoring or template-scoring runs. Overrides, reservations, reset receipts, and reset audit entries remain empty. Default policy remains 25 at revision 0. |

Fixture references: source `01a08071-8ae4-71fd-832a-ed82f20fd33c`; saved job `01a0806f-2535-7696-9ddd-3e8178f056ab` with three Qualifications and one Eligibility; résumé `01a08074-0d7f-706a-9706-b8ab73e147f8`; successful preview `01a08089-722a-77e9-a797-c0b2eb5580d6`. The saved import is `01a0806e-80a7-7c77-89b4-2ab21ced437f`.

This is a readiness observation, not recovery proof. The final drill can add live coverage for imports, selected source AI, structured library/résumé data, and the preview, but successful extraction, custom-template approval, saved-version/export, and quota-control records still need acceptance work before they can contribute populated recovery coverage. Backup and restore remain held for the release owner's final signal. Safe counts and statuses are retained in `test-results/v1.2-acceptance/recovery/follow-up-readiness.json`.

### Saved export and two successful scoring results

A later read-only inspection at 2026-09-08T10:28:34.976Z adds populated saved-version/export and scoring evidence to the earlier readiness snapshot. Checkpoint `01a0808b-842a-7cda-807e-3041f030bba8` captured résumé revision 3 with two structured sections at `2026-09-08T10:23:42.122Z`. Its document operation `01a0808b-842a-7df6-bd30-4ba242166f10` is Succeeded, stage PDF ready, with `validationPassed=true` and retained PDF/LaTeX/text/report keys.

The export was retained at `2026-09-08T10:24:10.782Z` under `river-evidence-export-v2`, with zero review issues. Export review digest: `65583a672298652fd696db753e8aafebae461928b360683675f95af66a0b6de9`. The release owner separately observed the rendered PDF and completed browser export; this read-only pass inspected metadata.

| Scoring run | Completed UTC | Simulations | Reservation | Administrator exempt |
| --- | --- | --- | --- | --- |
| `01a0808b-8473-7af3-bdff-d71c636a94e8` | `2026-09-08T10:24:05.856Z` | 6 | Consumed | Yes |
| `01a0808d-71d8-77e7-8679-6a0c2f3e8867` | `2026-09-08T10:26:03.481Z` | 6 | Consumed | Yes |

These are two distinct successful provider results, each completed on its first attempt with a distinct result digest and operation. The second explicit Score action created a new run; it is not evidence of retained-result reuse. The account's `2026-09-08` usage row is exactly 2, and its reservation ledger contains exactly two Consumed rows, both `exempt=1`. Six platform simulations count as one checkpoint scoring result. Administrator activity remains measurable while bypassing the daily cap; the default ordinary-account policy remains 25. This does not exercise ordinary-account exhaustion or a live allowance reset.

The retained artifact manifest records these SHA-256 values:

| Artifact | SHA-256 |
| --- | --- |
| PDF | `88310f3cec4030d39bcf6f905ee186e6db1f3ce3dc3ac9d6d8ca4bec1b6c580c` |
| LaTeX | `ba1200b0a529c457b742f37b1bdd1da98d702d9309056b6da6cd2ea32fa123da` |
| Plain text | `8a527da298af8097b28d239cfa210b1d43bb765198f40bc46d75b9b596924cf2` |
| Validation report | `57f9cfd97b006a23d0f53d900807b19e9a7e717b4510c8464f9769e44c092e44` |

These hashes were captured from the retained D1 manifest. The objects were not downloaded or rehashed by this metadata pass; that verification remains part of the final recovery drill. Safe IDs, statuses, times, counts, and digests are retained in `test-results/v1.2-acceptance/recovery/follow-up-checkpoint-scoring.json`. Backup and restore remain on hold.

## Hosted template specification and retained validation

Read-only D1 checks at `2026-09-08T11:50:56.361Z` and `2026-09-08T11:51:28.175Z` confirmed two distinct successful first-attempt `river-template-generation-v4` tasks:

| Step | Task | Operation | Proposal |
| --- | --- | --- | --- |
| First successful Recognition edit | `01a080d8-b8dc-7c07-bc83-1ef574562b31` | `01a080d8-b8dc-71b2-bd97-b177ec7f44d4` | `01a080d9-1bb8-7003-949b-b0258589e9fd` |
| Reapply after Undo | `01a080da-1e2a-7d9b-aec6-23fb8e7c7d03` | `01a080da-1e2a-7fb1-b21d-d08b492d5492` | `01a080da-8966-7a90-b04b-1c86465b34f7` |

Both stored candidate graphs have body size 11, a version 2 composition bundle, 12 schemas, 17 layouts, and exactly one optional `text` field named `recognition`. The Classic and Compact Experience Entry layouts each contain its slot exactly once and pin the corresponding schema revision. The first candidate uses Experience Entry revision 1; the reapply uses revision 2. Their proposal previews use transient storage. These successful previews alone are not retained-template recovery proof. The release owner separately verified both two-page PDFs, the Recognition sample text, Undo removing the field while preserving the last PDF, and the reapply.

Exactly one browser Save created design `01a080db-ef6c-705d-a67c-53c0c4c85d20` and revision `01a080db-ef6c-7b95-ba61-45141f7f24cc`, version 1. At `2026-09-08T11:54:18.392Z`, the saved revision was Approved and was the design's current revision. Its graph digest is `9033f4f1abcd10db0f38a56335ba92f4840fe5a9002023e90a2210b46a4d6110`. The saved graph retains body size 11, Classic's inherited Latin Modern Roman font, the version 2 bundle with 12 schemas/17 layouts, and optional Recognition in Experience Entry revision 2. Both Experience Entry layouts are revision 2, reference that exact schema revision, and render Recognition once.

Validation `01a080db-f1e2-7ad7-b45b-1043f7780913`, operation `01a080db-f1e2-70d6-9c0b-d040c2556bcd`, completed at `2026-09-08T11:53:23.472Z` on the first validation attempt with `approve_on_success=1`, operation Succeeded, and report passed. Report digest: `8df3ed598e776a6ffc191b2db94c1bbedd4a625828a5439e4ec51972197dead7`. All four fixtures passed; each fixture's first and second rendering fingerprints match, and all four PDF/LaTeX/text/report references use `retained/` with stored SHA-256 digests.

| Retained fixture | PDF SHA-256 |
| --- | --- |
| `all-types` | `a9afbd73c9e1d4a8c799b005d4814eab75b59ccc6b692053c1b809aa2f20f8a3` |
| `long-content` | `3b1741fe14b0f411d7a27bbcc31b58df293fae6fff4aa348ecbdb225183fa103` |
| `repeated-order` | `17ac1357b9cfb75923e8b7fbd6bb57cdb39d87b2f79d3e939d679dcd103cd6d8` |
| `unicode` | `08d18d698222adcd11e64c351a6272c72369e56007a6bd4ba1dd02b7022df420` |

All inspection statements wrote zero rows. Checks returned IDs, counts, statuses, expected specification values, and hashes; no credential or résumé/source payload was returned. These are stored-manifest hashes, not fresh object-byte rehashes. The final recovery drill must download and verify all 16 retained fixture artifacts, alongside the saved schema graph and historical tasks. Backup remains held for JavaScript-rendered public-URL acceptance after the next correction deployment.

## Final populated recovery — passed

The final backup and complete isolated recovery passed after hosted acceptance on staging commit `c27435724809bea10920d389d83f0582ba063084`. The release owner ran `pnpm backup:staging`, then `pnpm restore:drill <exact-absolute-receipt-path> --staging` from the unrestricted shell. Both exited 0. The observer independently ran the local table and populated-fixture verifiers after the drill's terminal success.

- Backup ID: `2026-09-08T12-25-34.792Z-379a1dac-1444-43b8-94cd-f2afc4fa43b6`.
- Snapshot created: `2026-09-08T12:25:41.900Z`.
- Snapshot: 646,340 bytes; SHA-256 `4819aacab6bbdc6d49a67db7ca396bedd9c7e006ddef646049285ba8b178ea31`.
- Private R2 key: `backups/database/staging/2026-09-08T12-25-34.792Z-379a1dac-1444-43b8-94cd-f2afc4fa43b6/snapshot.json.gz`.
- Exact receipt: `test-results/recovery/2026-09-08T12-25-34.792Z-379a1dac-1444-43b8-94cd-f2afc4fa43b6/receipt.json`.
- Exact drill report: `test-results/recovery/drill-3237ebd3-7383-4b07-be62-f1a4a7d02b9b/report.json`.
- Drill completed: `2026-09-08T12:30:35.327Z`; independent verification summary completed at `2026-09-08T12:32:31.345156Z`.

The snapshot contains all 82 base tables and 39 migrations through `0038_v1_2_trash.sql`. The complete drill checked 242 retained objects, 58 exact citations, six exported saved versions, and 12 retained template fixtures. No object subset was substituted. Schema/data hashes, pinned staging resources, integrity, foreign keys, and evidence FTS equality passed.

The independent raw-export comparison matched exact sorted-row hashes for all 82 restored base tables. JSON checks passed for structured library/résumé/checkpoint/template data and job imports. The snapshot now contains one composable template graph, one automatic-approval request, 12 job imports, and four saved source AI selections.

| Restored hosted fixture | Verified result |
| --- | --- |
| DOCX source `01a080b0-fc83-705b-b330-cdba832a4244` | Four accepted candidates resolve to four distinct claims and retained evidence revisions; selected AI reference retained. |
| PDF source `01a080b0-f6dd-759f-938f-e5a9630e4d95` | All 26 accepted candidates resolve to 26 distinct claims and retained revisions, including results after the twentieth item. |
| HTML-imported job `01a080b0-1a7f-74b0-877c-2f82a8b4be89` | Current saved workspace contains 32 requirements. |
| Pasted recovery job `01a080b5-e1e6-7489-b506-46a77adffa9b` | Current saved workspace contains ten requirements. |
| JavaScript-rendered job `01a080fa-6965-7f88-958f-4ae71f5f2599` | Saved import `01a080f9-a528-721b-87a6-9a971bb2548d` succeeded on its first attempt using browser retrieval, retaining 4,768 characters and 17 requirements: 14 Qualifications and three Eligibility. |
| Composable résumé `01a080af-1f9c-7786-9ee4-cb0fceb82aec` | Contact remains structured; full-table equality also preserves library/schema/template references and values. |
| Matching job `01a08095-be9e-772a-bedc-d45cb0bd4d18` | Four accepted associations use three distinct evidence items, and all referenced evidence revisions resolve. |
| Approved template revision `01a080db-ef6c-7b95-ba61-45141f7f24cc` | Current Approved version 1 retains optional Recognition, 11-point body text, 12 schemas/17 layouts, automatic approval on its first validation attempt, and four passing fixtures with 16 retained artifacts. |

The release owner separately reviewed both pages of the actual River Ashby import, edited the role/location summary, and saved all 17 requirements. The restored workspace and exact table comparison preserve that saved state without printing its private text.

All 242 downloaded object files were independently rehashed against the drill report. Of these, 198 also have stored expected digests: 26 source original/processing objects and 172 document/template manifest references. All matched. The other 44 legacy artifact references lack stored expected digests; they were downloaded, hashed, and validated by the applicable PDF/report/text checks, but cannot be described as matched to an absent historical digest. Every recovered reference uses `retained/`; archived source references remain included, while transient previews remain outside this retained-object drill.

Independent manifest comparison corroborated the standard drill's operation/template-validation/template-scoring hashes. The four earlier exported-checkpoint digests recorded above now match downloaded bytes, as do all 16 artifacts of the newly approved template. The recovery correction retains optional manifest digests and accepts legacy null inventory digests without weakening exact key/kind/count or any available record-digest checks. Four focused recovery tests passed, including legacy inventory compatibility and conflicting references.

The default scoring policy remains 25 at revision 0. Three usage-day rows total eight results: six historical results and the two current administrator results. Both named scoring runs above retain distinct Succeeded operations and Consumed reservations with `exempt=1`; their account's `2026-09-08` usage remains exactly two. Admission, settlement, and release trigger hashes are preserved, all ranges are valid, and no Reserved row points to a terminal operation. Overrides, reset receipts, and reset audit entries remain empty. Their live reset/exhaustion behavior is therefore not claimed; focused synthetic tests cover those paths.

Safe evidence is under ignored `test-results/v1.2-acceptance/recovery/`: `final-populated-backup.log`, `final-populated-restore.log`, `final-populated-invariants.json`, `final-populated-fixtures.json`, and `final-populated-summary.json`. The two local verifiers are `verify-restored.mjs` and `verify-final-populated.mjs`; their hashes are included in the summary.

| Evidence file | SHA-256 |
| --- | --- |
| Final receipt | `ba737663548efd21edd137d4732c95a0ffcec4f2478edb621cc8c7f5e980b0d5` |
| Drill report | `2dce02f77410e47120f6b05924c8532a40953384e57e622638f60cc453ce65bb` |
| Exact-table invariants | `d4f42f98508d7ab69307c862381eae86543ae0ff803c5e49e0322ac7c76a477f` |
| Populated-fixture verification | `f7df67d4fd6f53b839087c47ee3cd8026ad81a0659aec2e56eadf90d8535f892` |
| Final summary | `470f94300429aa0aa78f39738c21258f2d35dbd2d39ee9dd8bf29aabc8cd2549` |

Read-only lifecycle inspection at `2026-09-08T11:14:14.157Z` confirmed 30-day backup expiry, seven-day expiry for the three transient preview prefixes, and seven-day incomplete multipart cleanup. No expiry rule targets `retained/` or the whole bucket. This recovery did not mutate lifecycle rules or delete remote objects. Object downloads retained the bounded three-attempt read policy with 500 ms and 1,000 ms waits.

The backup removed only its own temporary `data.sql` and `validation.sqlite`; the verifier confirmed they are absent. Snapshot/receipt, restored SQLite/report, and every downloaded object remain preserved. Directories are mode 0700 and private files 0600. No extra cleanup ran, no restored work was dispatched, and no remote database was restored. The matching AI encryption key was not fetched, printed, changed, or decrypted. Production was untouched.

## Initial deployment

Cloudflare build `b810696e-618a-4638-9c21-8e1b26afc39d` was created at `2026-09-08T09:13:06.283Z` for that exact staging commit and completed successfully at `2026-09-08T09:23:29.787Z`. Its commands were `pnpm ci:build staging` and `pnpm ci:deploy staging`. The build log confirmed 261 passing Workers tests, 43 passing template tests, and ten successful test tasks. The final deploy log identifies the current web version and confirms its sign-in and anonymous API checks. No manual deployment or retry was started during this observation.

### Initial deployment resources

Independent Cloudflare API reads confirmed both active deployments at 100 percent:

| Worker | Deployment | Version | Created UTC |
| --- | --- | --- | --- |
| `river-documents-staging` | `b2ff81d2-6361-4835-ae49-ef3106e23298` | `20bece6c-0688-48e4-85c4-b2db7e184395` | `2026-09-08T09:23:02.941685Z` |
| `river-staging` | `d0d29e01-b3b8-4f3b-ae41-7f643dee0058` | `1a95dbba-6d5a-4d16-8a00-eed11f8ab13b` | `2026-09-08T09:23:25.865862Z` |

Live settings match the staging source configuration: D1 `1b837147-de99-4aaa-a4ad-386b826412b0`, R2 `river-staging-artifacts`, `DOCUMENTS` service `river-documents-staging` with `DocumentService`, `BROWSER`, and all eleven workflow names/classes. The backup account/database/bucket tuple also matches. Document Worker `CONTAINER` remains bound to `DocumentContainer`, namespace `884d8dd4da43419286b646a4dc1c253b`. Both Workers use compatibility date `2026-08-22` and `nodejs_compat`.

Query-string redaction is enabled; persistent/invocation logs and traces are disabled on the web Worker, with `river-telemetry-staging` as its tail consumer. The `AI_CREDENTIAL_ENCRYPTION_KEY` secret binding is present. Its value was not fetched, printed, or changed. Preserving that matching key remains required to use restored encrypted connections; this drill does not decrypt or reconnect them.

D1 reports 39 applied migrations. All five v1.2 migrations (`0034` through `0038`) applied at `2026-09-08 09:22:56 UTC`. The independent migration reads wrote zero rows.

At `2026-09-08T09:25:09Z`, public route checks returned `/sign-in` 200, `/docs` 200, and anonymous `/api/v1/me` 401. Anonymous `/admin` and `/trash` redirected to `/sign-in`, which returned 200. These checks do not establish signed-in administration or browser/provider acceptance.

## Initial post-migration backup and restore

The first manual backup attempt failed at its initial Wrangler D1 read, before export. Its output was withheld by the existing recovery wrapper. A read-only `SELECT 1` probe succeeded, and one fresh backup retry succeeded. No additional cause was recovered; the initial failure must not be characterized as an application data error.

- Backup ID: `2026-09-08T09-27-14.507Z-fd60e257-59e7-42a3-b3e5-d51c6ee4660e`.
- Private R2 key: `backups/database/staging/2026-09-08T09-27-14.507Z-fd60e257-59e7-42a3-b3e5-d51c6ee4660e/snapshot.json.gz`.
- Snapshot SHA-256: `66f3bedff5093c7e4a86bba4bb0fbac19692ffaed76963ec4adc79a1dd551308`.
- Captured base tables: 82, including all v1.2 tables and migrations through `0038`.
- Receipt: `test-results/recovery/2026-09-08T09-27-14.507Z-fd60e257-59e7-42a3-b3e5-d51c6ee4660e/receipt.json`.
- Isolated restore directory: `test-results/recovery/drill-d4db41cc-ebc8-48b0-9abc-540a9765befc/`.

`pnpm restore:drill <absolute-receipt-path> --staging` completed at `2026-09-08T09:30:59.609Z` with exit 0 and `status: passed`. It restored all 82 tables and verified all 170 retained objects, 27 exact citations, five exported saved versions, and eight retained template validation fixtures. Its final report is `test-results/recovery/drill-d4db41cc-ebc8-48b0-9abc-540a9765befc/report.json`, SHA-256 `20a69e688f77dc320d295c24001bea4195fa62883f3e2cdbc68a00aa7bf7e7f6`. Object reads retained the bounded three-attempt recovery policy. The restored SQLite file remains local and unpublished, with no restored work dispatched.

An independent comparison loaded the raw export into an in-memory database without import-time triggers and compared sorted row hashes against the read-only restored database. All 82 base tables matched exactly. Integrity and foreign-key checks passed. Template/library/résumé and job-import JSON columns remained valid. This verifies stored JSON preservation without printing private values.

The restored quota policy retains the default limit 25 at revision 0. Two usage-day rows contain six historically seeded results. Account overrides, reservations, reset receipts, and reset audit entries are empty and remain empty. Admission, settlement, and release triggers are present, their hashes are retained, ranges are valid, and no Reserved entry points to a terminal operation.

This staging snapshot has no v1.2 composable template graph, URL/paste job-import record, automatic-approval request, or saved source model selection. It therefore establishes additive migration, historical preservation, and empty-state recovery for those new features. Populated v1.2 schema/override/reset/reservation behavior is covered separately by the focused synthetic recovery/scoring tests; it is not claimed as live use in this snapshot.

Safe acceptance evidence is retained under ignored `test-results/v1.2-acceptance/recovery/`. Private SQL and source/artifact bytes remain under the existing restricted recovery directories. Signed-in browser/provider acceptance is recorded separately by the release owner.

Evidence files: `deployments-bindings.json`, `migrations.json`, `routes.json`, `post-backup.log`, `d1-probe.log`, `post-backup-retry.log`, `post-restore.log`, `restored-invariants.json`, and `summary.json`. The summary retains receipt/report/invariant-file hashes. The ignored `verify-restored.mjs` records the exact read-only comparison and invariant queries used for this drill. No tracked application or recovery code changed in this verification task.
