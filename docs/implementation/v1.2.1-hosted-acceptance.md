# River v1.2.1 hosted acceptance

Status: in progress on staging. Sixteen of eighteen normal-workflow exports passed content and visual comparison. The final code corrections are deployed and their Paper screenshot comparison passed. Production is outside scope.

## Content and automatic upgrades

Three separate comparison drafts were assembled through the signed-in application: the owner's saved personal facts, fictional Maya Chen, and fictional Jordan Alvarez. The existing personal résumé was not overwritten. Fictional source and Skill records are labeled benchmark-only and not Owner evidence.

The read-only content verifier passed for all three drafts. It compares every schema-visible value, date precision, and section position against the frozen reference inputs. The personal comparison has five sections, Maya six, and Jordan seven. Receipts are under ignored `test-results/v1.2.1-acceptance/` and `test-results/v1.2.1-comparison/hosted-content-verification.json`.

The first load on v1.2.1 upgraded unchanged built-in definitions to revision 2 and advanced each draft once. Deep comparison confirmed identical content and evidence references. A second reload left revisions unchanged. This owner had no existing custom-bound editable draft to exercise live; custom/source-matching preservation is covered by focused automated tests.

## Editor and preview

- Existing sections, groups, and entries initially collapsed with useful summaries. Inserted sections and a new experience entry opened automatically.
- Keyboard Enter toggled a disclosure. Typed Employer/Title values survived closing and reopening. Canceling the insertion dialog left the comparison unchanged.
- Clearing the required Name field with the keyboard and immediately collapsing Contact caused autosave validation to reopen it, show `Complete name.`, and focus Name. The original name was restored.
- Month/year values entered as `June 2024`, `May 2023`, `August 2023`, and ISO year-month forms retained their precision through normal insertion. The owner's legacy whole date range stayed unchanged.
- Contact URL remained required. Display text lived under Additional settings. Removing the label rendered the URL once; restoring the label restored the named link.
- Successive profile edits produced the final text with no obsolete intermediate text in the rendered preview. One measured Paged.js pass took 74 ms; this is pagination time, not an end-to-end typing latency benchmark. The configured debounce is 350 ms.
- Preview uses a local `srcDoc` iframe. No external preview URL is used. Valid section insertion succeeded while the hosted PDF runtime was failing.

## Saved-version dialogs

Scoring showed readiness, allowance, Cancel and Score. Refinement showed the instruction field, a compact current-model label with Change, Cancel and Suggest changes. Both correctly required a prepared PDF. Saved refinements appeared on the checkpoint's Refinements tab. The existing `scores=true` link selected Scores and displayed its history. Live refinement completion remains pending.

## Hosted compiler diagnosis

The first Maya checkpoint and two explicit retries failed. The normal runtime fixture also failed. The same captured fictional checkpoint input compiled locally with text validation passing and one page.

Bounded staging document logs showed an HTTP 422 response, followed by an unresolved service RPC during response cancellation. After the container's idle cycle, the synthetic runtime fixture succeeded in 2.80 seconds and the identical Maya comparison succeeded in 7.39 seconds. This supports a transient post-deployment runtime state; it does not establish the exact underlying failure stage. The transport follow-up drains the fixed error response inside the Durable Object request and records only the allowlisted stage for future diagnosis. Sixteen subsequent normal-workflow exports passed content validation and visual inspection of all twenty-two exported pages. Both personal Technical combinations remain outstanding.

An interactive staging container shell was rejected by automatic approval review because it would grant broader access than this diagnosis needs. Investigation continued with bounded logs and targeted code. No shell workaround was used.

## Paper fidelity

The revised Paper editor includes the nested Entries disclosure, section Heading field, existing Résumé settings control, preview caveat, and Add/Create actions. Its final design review passed spacing, typography, contrast, alignment, and artboard fit. Current reference: `v1.2.1-paper/02-editor-current.jpg`.

The implementation preserves Paper's 32px gap and 540px editor maximum, while scaling the editor to 43% of the workspace between the app navigation and preview. Top-level section titles use Paper's 18px size. The persistent global navigation is shared app chrome outside these focused artboards. Final hosted screenshots passed comparison after deployment: `v1.2.1-hosted/02-editor-final.jpg` and `v1.2.1-hosted/03-job-requirements-final.jpg`. The browser confirmed all six top-level section headings at 18px and reported no errors on either screen.

During rapid save-version testing, an autosave acknowledgment changed the form's React key and cleared the optional version name. The form now retains its identity while receiving the latest acknowledged revision; retry requests still retain their original idempotency key. Hosted verification of this fix is pending.

## Refinement provider timeout

The full structured Maya checkpoint passed preparation and reached the configured OpenAI model. Three attempts across two tasks exceeded the existing 60-second limit, including a request using GPT-4.1. Workflow history recorded the bounded timeout category, but the refinement page replaced it with a generic interruption message. Refinement now uses the shared safe AI Workflow boundary and displays the fixed actionable provider message. New tasks allow 150 seconds for complete source and intended-text output inside a three-minute generation step; historical profiles retain their original 60-second limit. The 17 focused refinement/AI Workflow tests and ten source-proposal tests pass.

The intermediate staging follow-up completed successfully at `2026-09-08T20:04:24.710Z`: commit `1d7a1eb40999d3080ef937a2c3fcd3a86b1ce3f0`, build `34eaaa57-abbc-4f9d-902d-7c8ac19671c4`, web version `03638e7c-f915-43a4-a384-f920dc9bf7bc`. Its integrated checks, full container fixtures, deployment and sign-in/anonymous-API probes passed.

The final corrections passed both required application/document CI runs and were promoted through [PR #30](https://github.com/joshuasilva414/river-resume-builder/pull/30) and [PR #31](https://github.com/joshuasilva414/river-resume-builder/pull/31). Staging commit `dbdc712b3ca8a8243f1358d3841db063d844c2b9` merged at `2026-09-08T20:32:04Z`. Cloudflare build `201b53cd-918a-4c51-abcb-50c1579558e0` completed successfully at `2026-09-08T20:48:17.791Z`. Web version: `2036339c-9946-4ad8-89ae-6ebf456f071f`; document Worker: `0611fc5c-45d1-4855-92dc-e8e858510f95`; telemetry Worker: `c06ff0fb-0116-4286-88f3-f9b60054ecfc`. Its integrated checks, compiler fixtures, backup, no-migration check, deployment and hosted route probes passed.

## Additional browser checks

The library's existing fictional Experience section initially collapsed its top-level form, Entries group, and both entries independently. Keyboard Enter opened the top-level disclosure. Both entries remained open together. Canceling the editor preserved the saved library revision.

The existing `scores=true` link selects the checkpoint's Scores tab and shows scoring history. Job résumé rows contain only name, saved time, and Open. Successful job analysis displayed its proposed requirements directly, without a completed-generation status panel; the suggestions were not applied.

The first job screenshot exposed a fidelity gap: metadata, wording, and evidence actions occupied three stacked rows. Paper now includes a 620px requirement-column variant for the persistent saved-posting layout. The implementation groups wording/secondary details and aligns fixed-width selection, evidence, and Edit slots. At 720px of row width they share a horizontal row. The exact 12px padding, 16px action gaps, and 100/152/44px slots come from Paper. The final hosted row was 625px wide with 12px vertical padding, 16px/24px medium wording, 14px/20px secondary details, and 152px/44px evidence/Edit buttons. The narrow Paper variant now uses the app’s shared 13px semibold compact action style. Visual review passed spacing, typography, contrast, alignment, row rhythm and fit. Design reference: `v1.2.1-paper/01-narrow-requirement.jpg`.

Live job analysis on the synthetic posting reached the compact `Analyzing job…` state with Cancel and then displayed successful proposed requirements directly. The model completed before the separate cancellation click. A subsequent immediate-cancellation probe was blocked by automatic approval review as a new provider submission, so it was not retried through another path. Late-result suppression is verified by the focused repository test `rejects invalid citations, discards rejected generated payloads, and suppresses success after cancellation` in `apps/web/test/job-ai.test.ts`; it cancels the operation and asserts that publication returns null with no saved proposal. This is deterministic cancellation evidence, not a claim of a completed live cancellation.

## Remaining acceptance and authorization

The owner explicitly approved downloading personal comparison PDFs and validation reports for local comparison. All four completed personal exports and the twelve fictional exports are retained and verified. Automatic approval review rejected the subsequent personal Technical-theme change because it exceeded the download-only approval. The separate approval request for finishing comparison draft changes/exports, reviewing and accepting a layout refinement, and a staging backup/local restore remains unanswered. These steps were not retried through another route. The version-name fix is deployed and checked in CI; exercising it across a new autosave remains part of the pending workflow acceptance.

Deployment retained backup `2026-09-08T20-47-06.781Z-808aac80-24af-41f0-9784-fb8eb6c64d16`, SHA-256 `dec34cc5641f96cd5ae3241497e5433b0ca90df8996f1bf3e0b34b7fad443cea`. This is the automated pre-deploy backup after sixteen exports, not a completed post-acceptance restore drill.
