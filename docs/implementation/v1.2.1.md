# River v1.2.1 delivery

Status: implementation and integrated checks pass; the final code is deployed to staging and its Paper comparison passed. Acceptance remains incomplete. Sixteen of eighteen normal-workflow export comparisons pass. Live refinement completion and the post-acceptance restore drill remain outstanding. Production is outside scope.

The approved scope is captured in the task handoff. Baseline: staging `fb27587b0e295ce5fd3acc3283d0590403419b39`, matching the completed v1.2 source tree at `8f7db704e0a3e6e5823625c53154791bfb8a9796`. Work is isolated on `feature/river-v1.2.1`; the previous checkout is preserved.

## Acceptance checklist

- [x] Establish current staging baseline without modifying the previous checkout.
- [x] Design revised UI in Paper before implementation.
- [x] Simplify job résumé rows and generation states. Live running/success states passed; deterministic tests verify cancellation suppresses late publication.
- [x] Verify the final compact requirement rows against Paper on the new staging deployment.
- [x] Focus scoring/refinement launch dialogs; retain saved results/history in checkpoint tabs and preserve `scores=true`.
- [x] Verify independent accessible section/group/entry disclosures, automatic opening for new items, preserved unsaved content and validation focus in résumé/library editors.
- [x] Verify inferred date precision, preserved legacy range text, optional link labels and single rendering.
- [x] Verify local approximate HTML preview, lazy Paged.js, debounced pagination and obsolete-result rejection; valid content insertion succeeds independently of PDF readiness.
- [x] Implement improved built-in layouts and distinct Classic/Compact entries while preserving Classic/Minimal/Technical themes.
- [x] Verify versioned, source-matched, idempotent editable-draft upgrades preserve content/evidence. Focused tests cover concurrent edits and customized definitions; live library checks preserve old revisions.
- [x] Repair recursive refinement field mapping and actionable preparation failures; structured/mixed/nested and checkpoint tests pass. Live requests pass preparation and reach the provider.
- [ ] Complete a live layout refinement, review it, accept a new checkpoint and export it. The original 60-second provider timeout prevented completion; the final correction allows 150 seconds for new tasks.
- [ ] Finish all eighteen normal-workflow reference comparisons. Sixteen exports and all twenty-two exported pages pass; personal Technical/Classic and Technical/Compact remain.
- [x] Capture and compare final hosted/Paper screenshots; confirm 18px section headings and aligned compact requirement actions.
- [ ] Exercise the deployed version-name correction across a new autosave. CI passes; the workflow check remains pending.
- [x] Pass focused and integrated checks, complete image builds and real compiler fixtures in CI.
- [ ] Complete post-acceptance backup/isolated restore and verify preservation of all 270 pre-release retained objects.
- [ ] Synchronize final delivery evidence and complete staging acceptance.

## Paper design source

[River v1.2.1 · Editing and document quality](https://app.paper.design/file/01M1PCGGJYH2EC4YRJNZSPRDZK/7-0). Existing release pages are preserved. All examples are fictional.

| Surface | Artboard | Design review |
| --- | --- | --- |
| Job requirements, generation states, résumé rows | `C6B-0` | 12px row padding and aligned fixed-width action slots. Includes a 620px requirement-column variant for the saved-posting layout. |
| Collapsible résumé editing and approximate preview | `C6C-0` | Editor maximum 540px, 32px gap, 18px section headings, independent disclosures and visible actions. |
| Focused launch dialogs and saved-version tabs | `C6D-0` | 600px dialog, 24px padding/gaps, 28/34px headings; history outside launch dialogs. |
| Content forms, dates, links, insertion | `C6E-0` | Essential fields first, optional display text, independent Links disclosure; insertion does not wait for PDF. |

The design uses existing Paper/ink/editor/rule tokens, Newsreader headings and Instrument Sans controls. Computed styles and representative JSX informed implementation. Paper screenshots passed hierarchy, contrast, alignment, spacing and clipping review. Current references are [the editor](v1.2.1-paper/02-editor-current.jpg) and [the narrow requirement row](v1.2.1-paper/01-narrow-requirement.jpg).

The persistent global app navigation sits outside the focused artboards. At the actual 1280px browser width, the editor scales to 43% of available workspace width, bounded between 360px and 540px, with the 32px gap preserved. This gives the preview more usable space. Final browser/Paper inspection passed after deployment. The six top-level headings are 18px. Requirement rows use 12px vertical padding and fixed 152px/44px evidence/Edit actions. Paper records the shared 13px semibold compact button style. Screenshots: [editor](v1.2.1-hosted/02-editor-final.jpg) and [requirements](v1.2.1-hosted/03-job-requirements-final.jpg). No browser errors were reported.

## Behavior and contracts

The editing preview renders structured data directly as approximate HTML/CSS. Paged.js 0.4.3 loads locally and paginates after a 350ms debounce, using a serial queue and obsolete-result rejection. Escaped current content appears before pagination. A measured Paged.js pass took 74ms; this is pagination time, not an end-to-end typing benchmark. [Paged.js FAQ](https://pagedjs.org/en/faq/) and [developer documentation](https://pagedjs.org/devdocs/) document the controlled Previewer API.

Valid saves and insertion require structural validation but do not wait for PDF compilation or viewer readiness. Exact saved-version PDF review and export validation continue to use LaTeX/Tectonic. No new preview service receives résumé content. This supersedes the earlier PDF-before-insertion requirement and deferred-HTML-preview statement.

Editable drafts using exactly unchanged built-in identities and sources upgrade automatically. Customized definitions remain intact. Library revisions, checkpoints and historical exports remain immutable. Normal library editing/insertion can use updated compatible definitions without rewriting the old revision.

The synthetic Contact/Experience regression first reproduced `Structured field metadata is incomplete.` A shared domain traversal now supplies rendered text, stable paths, required markers and evidence for both render locators and refinement. Mixed legacy/structured fields retain their original identities. The mapper accepts the existing structured model's 200 evidence references and longer nested paths within the existing total input bounds. Preparation failures return bounded actionable messages.

New refinement tasks allow 150 seconds for model generation inside a three-minute Workflow step. Historical captured profiles retain their 60-second setting. The shared safe Workflow boundary preserves fixed actionable provider errors. This follows three live timeout attempts that successfully passed the repaired preparation stage but produced no completed proposal.

The optional saved-version name now survives an autosave acknowledgment because the form retains its identity while receiving the latest acknowledged draft revision. Capacity retries retain their original request and idempotency key. Final hosted verification is outstanding.

## Validation and document quality

The integrated suite passed: 277 Workers tests, all package test tasks, five recovery/cutover tests, TypeScript and lint across six packages, staging bundle build and Wrangler dry run. Template coverage includes 47 cases. The final refinement changes passed 17 focused Workers/Workflow tests and ten source-proposal tests. Both application and document CI runs passed before PR #31 was merged.

Date inference has 15 focused cases for year, month/year, ISO calendar dates, Present, invalid dates and ambiguous ranges. Live browser checks covered disclosures, keyboard interaction, validation focus, URL fallback, precision preservation, generation/launch dialogs, score history links and insertion during compiler failure. The detailed record states the limits of each check: [hosted acceptance](v1.2.1-hosted-acceptance.md).

Three frozen datasets use existing saved personal facts, fictional one-page Maya Chen and fictional two-page Jordan Alvarez. Independent ReportLab references preserve identical wording and order. Fictional source/Skill evidence is explicitly benchmark-only and not Owner evidence. The personal comparison is separate from the existing résumé.

All eighteen direct-compiler comparisons passed text validation and intended page counts and were visually inspected. Sixteen normal-workflow exports currently pass retained-export identity, exact wording/order, every nested entry layout, hashes, renderer validation and intended page count. All twenty-two exported pages and four reference pages were reviewed; minimum body text is 10 TeX points, with zero out-of-bounds glyphs. Full findings and ignored local artifact locations are in [document quality](v1.2.1-quality.md). Private résumé PDFs and reports are not committed.

The initial hosted compiler incident returned HTTP 422 and hung while canceling its error response. The same captured fictional input compiled locally. After the container's idle cycle, the normal runtime fixture and all sixteen subsequent exports passed. This supports a transient post-deployment runtime state; the exact underlying failure stage remains unconfirmed. Transport diagnostics now drain the fixed error response and log only the allowlisted stage, without document content. The [hosted record](v1.2.1-hosted-acceptance.md) retains the evidence and access limitations.

## Staging delivery and recovery

| Promotion | Staging commit | Hosted result |
| --- | --- | --- |
| [PR #26](https://github.com/joshuasilva414/river-resume-builder/pull/26) → dev; [PR #27](https://github.com/joshuasilva414/river-resume-builder/pull/27) → staging | `4fac19a665b986daf8a9edbee49ecd21b4c4cd33` | Build `c9f015fd-3fb4-4704-890a-c01485990b9b` passed at 19:16:38 UTC. |
| [PR #28](https://github.com/joshuasilva414/river-resume-builder/pull/28) → dev; [PR #29](https://github.com/joshuasilva414/river-resume-builder/pull/29) → staging | `1d7a1eb40999d3080ef937a2c3fcd3a86b1ce3f0` | Build `34eaaa57-abbc-4f9d-902d-7c8ac19671c4` passed at 20:04:24 UTC; web `03638e7c-f915-43a4-a384-f920dc9bf7bc`. |
| [PR #30](https://github.com/joshuasilva414/river-resume-builder/pull/30) → dev; [PR #31](https://github.com/joshuasilva414/river-resume-builder/pull/31) → staging | `dbdc712b3ca8a8243f1358d3841db063d844c2b9` | Build `201b53cd-918a-4c51-abcb-50c1579558e0` passed at 20:48:17 UTC; web `2036339c-9946-4ad8-89ae-6ebf456f071f`. Hosted route probes and final UI screenshot comparison passed. |

Completed hosted builds independently ran integrated checks, full container/compiler fixtures, backup/migration checks, document Worker deployment before web, and `/sign-in` = 200 plus anonymous `/api/v1/me` = 401. This release adds no migration. Neither main nor production has been promoted.

Pre-deployment backup `2026-09-08T18-23-10.755Z-16c44847-7f38-4349-8046-f906e175c8d3` retained all 82 base tables. Snapshot SHA-256: `0aa654658799e993a7905e4f36a29213d6293005b4761efa0e77b0ffdba039e0`.

The pre-deployment isolated restore passed at `2026-09-08T18:36:12.192Z`: 82 base tables, 270 retained objects, 58 citations and six exported checkpoints. Report: `test-results/recovery/drill-41a85276-008c-44bf-8c3f-82af305180fa/report.json`. Restore uses local SQLite and does not overwrite the hosted database. The post-acceptance drill and historical object-hash comparison remain outstanding. Full recovery also requires retaining the matching AI-connection encryption key; this record does not claim a live credential restore.

The final deployment automatically retained backup `2026-09-08T20-47-06.781Z-808aac80-24af-41f0-9784-fb8eb6c64d16`, SHA-256 `dec34cc5641f96cd5ae3241497e5433b0ca90df8996f1bf3e0b34b7fad443cea`, after sixteen exports. This does not replace the outstanding post-acceptance restore drill.

The remaining personal layout/export, refinement acceptance, and backup/local restore workflow awaits the owner's separate approval after automatic approval review rejected a personal theme change as outside the download-only approval. Personal downloads are approved and complete for the four existing exports. Full acceptance and final documentation promotion remain open.
